# Local config — DO NOT REMOVE. Each contributor must edit lib/config.js with
# their own Firebase URL. Don't commit local edits.
# (We do commit the file as a template — uncomment the line below if you fork
#  and want to track config separately.)
# lib/config.js

# OS
.DS_Store
Thumbs.db
*.swp
*~

# Editors
.vscode/
.idea/
*.code-workspace

# Build / package artifacts
*.zip
*.tar.gz
dist/
build/
node_modules/
package-lock.json
yarn.lock

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Lab / pen-test artifacts (not part of the released extension)
cw_lab/
tests/
report/
